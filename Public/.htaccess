RewriteEngine On
# Si el archivo o carpeta existe, úsalo directamente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
# Si no existe, manda todo a index.php
RewriteRule ^ index.php [QSA,L]